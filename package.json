{
  "name": "choirdb",
  "version": "0.0.1",
  "description": "choir database",
  "main": "index.js",
  "scripts": {
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "build": "cd client && npm run build",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy:full": "npm run build && git add . && git commit -m new_build && git push origin master"
  },
  "author": "CLG",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
